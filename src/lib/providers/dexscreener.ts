import{num}from"@/lib/utils";export async function getDexData(address:string){const r=await fetch(`https://api.dexscreener.com/token-pairs/v1/base/${address}`,{next:{revalidate:60},signal:AbortSignal.timeout(8000)});if(!r.ok)throw new Error(`DexScreener returned ${r.status}`);const pairs=await r.json()as any[];if(!Array.isArray(pairs)||!pairs.length)return null;const p=pairs.filter(x=>x.chainId==="base").sort((a,b)=>(num(b.liquidity?.usd)??0)-(num(a.liquidity?.usd)??0))[0];if(!p)return null;const isBase=p.baseToken?.address?.toLowerCase()===address.toLowerCase(),token=isBase?p.baseToken:p.quoteToken,socials=Array.isArray(p.info?.socials)?p.info.socials:[],websites=Array.isArray(p.info?.websites)?p.info.websites:[];const s=(platform:string)=>socials.find((x:any)=>String(x.platform).toLowerCase()===platform)?.url||null;return{name:token?.name??null,symbol:token?.symbol??null,logo:p.info?.imageUrl??null,priceUsd:num(p.priceUsd),marketCap:num(p.marketCap),fdv:num(p.fdv),liquidityUsd:num(p.liquidity?.usd),pairCreatedAt:num(p.pairCreatedAt),dex:p.dexId??null,pairAddress:p.pairAddress??null,pairUrl:p.url??null,volume24h:num(p.volume?.h24),volume6h:num(p.volume?.h6),volume1h:num(p.volume?.h1),change24h:num(p.priceChange?.h24),change6h:num(p.priceChange?.h6),change1h:num(p.priceChange?.h1),buys24h:num(p.txns?.h24?.buys),sells24h:num(p.txns?.h24?.sells),buys6h:num(p.txns?.h6?.buys),sells6h:num(p.txns?.h6?.sells),buys1h:num(p.txns?.h1?.buys),sells1h:num(p.txns?.h1?.sells),website:websites[0]?.url??null,twitter:s("twitter"),telegram:s("telegram"),farcaster:s("farcaster"),otherSocials:[]}}
