import{NextRequest,NextResponse}from"next/server";import{analyzeTokenData}from"@/lib/analyzer";import{runTokenAgent}from"@/lib/agent";export const runtime="nodejs";export async function POST(req:NextRequest){try{const{contract}=await req.json(),data=await analyzeTokenData(String(contract||""));let agent=null,agentError:string|undefined;try{agent=await runTokenAgent(data);if(!agent&&!process.env.OPENAI_API_KEY)agentError="OpenAI is not configured."}catch(e){agentError=e instanceof Error?e.message:"Agent analysis failed"}return NextResponse.json({data,agent,agentError},{headers:{"Cache-Control":"s-maxage=120, stale-while-revalidate=300"}})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Analysis failed"},{status:400})}}
