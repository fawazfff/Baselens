import type { Metadata } from "next";
import Link from "next/link";
export const metadata:Metadata={title:"How it works",description:"How BaseLens collects Base token evidence, calculates deterministic scores and uses AI to explain the result."};
export default function Page(){return <main className="doc shell" id="main-content"><nav className="breadcrumbs" aria-label="Breadcrumb"><Link href="/">BaseLens</Link><span>/</span><span>How it works</span></nav><div className="eyebrow">Agent architecture</div><h1>How BaseLens works</h1><p>BaseLens separates data collection from AI interpretation. The model never receives the job of inventing or fetching live token statistics.</p>{[["1. Observe","Validate the contract on Base, read ERC-20 metadata, find live DEX pools, then request security and holder signals."],["2. Analyze","Deterministic scoring measures market health, liquidity, activity, holders, contract safety, social presence and momentum."],["3. Reason","Only normalized evidence and calculated scores are sent server-side to OpenAI. Raw provider payloads are stripped first."],["4. Decide","The agent returns a structured verdict such as Promising, Watchlist or High Risk without pretending to predict future price."],["5. Explain","The UI shows concise positive signals, risk signals and items worth investigating, not private chain-of-thought."]].map(([h,p])=><section key={h}><h2>{h}</h2><p>{p}</p></section>)}<div className="formula">USER → OBSERVE → ANALYZE → REASON → DECIDE → EXPLAIN</div><p className="doc-links">See <Link href="/methodology">how scores are calculated</Link> or <Link href="/analyze">start a token analysis</Link>.</p></main>}
