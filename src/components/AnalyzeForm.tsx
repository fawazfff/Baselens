"use client";import{useRouter}from"next/navigation";import{useState}from"react";import Link from"next/link";export function AnalyzeForm(){const[v,setV]=useState(""),[err,setErr]=useState(""),router=useRouter();function go(){const x=v.trim();if(!/^0x[a-fA-F0-9]{40}$/.test(x)){setErr("Enter a valid 0x Base token contract address.");return}router.push(`/analyze/${x}`)}return <div><div className="searchbar"><input className="input" value={v} onChange={e=>{setV(e.target.value);setErr("")}} onKeyDown={e=>e.key==="Enter"&&go()} placeholder="Paste a Base token contract address  0x…"/><button className="btn blue" onClick={go}>Analyze Token</button></div>{err&&<div className="error">{err}</div>}<div className="hero-actions"><Link href="/compare" className="btn ghost link">Compare Tokens</Link><span className="muted small">Base mainnet · Chain ID 8453</span></div></div>}
